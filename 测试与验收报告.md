# 成员 B 测试与验收报告

## 1. 验收范围

仅检查成员 B 的 MQTT 外层封装、主题、传输调度、审计和部署资料，不测试或包含密码协议。

## 2. 自动测试结果

执行命令：

```powershell
python -m unittest discover -s tests -v
```

结果：

```text
Ran 7 tests
OK
```

通过项目：

1. Envelope 编码、解码及主题一致性。
2. 未知 JSON 字段和重复 JSON 字段拒绝。
3. QoS 重复握手只调用处理器一次。
4. 相同消息 ID、不同 payload 冲突拒绝。
5. 乱序应用记录按序交付。
6. 乱序缓冲区有界。
7. 审计日志拒绝嵌套敏感字段。

## 3. 演示结果

执行：

```powershell
python demo.py
```

验证：

- 成功生成 ClientHello MQTT 主题；
- 重复握手实际只处理一次；
- 先收到序号 1、后收到序号 0 时，最终按 `[0, 1]` 交付；
- 成功输出一条不含秘密的 JSON 审计事件。

## 4. 目录边界检查

以下目录均不存在：

```text
crypto = false
security = false
gateway = false
.git = false
```

因此本包不包含原项目密码实现、主线安全状态机、网关业务代码或仓库历史。

## 5. 交付结论

本包满足成员 B 的交付要求：

- 主题表、消息字段表和非敏感示例；
- 消息时序与重复/乱序处理建议；
- 可运行的 Envelope、主题、调度与审计代码；
- 审计事件字段和部署说明；
- Mosquitto 本地配置及 ACL；
- 自动化测试与演示。
